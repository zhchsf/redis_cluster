require 'spec_helper'

describe RedisCluster do
  it 'has a version number' do
    expect(RedisCluster::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
